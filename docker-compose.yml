version: '3.8'
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: user_aurum
      POSTGRES_PASSWORD: password_rahasia
      POSTGRES_DB: aurum_db
    ports:
      - '5432:5432'